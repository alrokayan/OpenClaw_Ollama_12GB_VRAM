# TOOLS.md - This machine

- OS: Windows 11. Your `exec` tool runs commands on this host (PowerShell / cmd).
- Android SDK is installed; `adb` and `emulator` are on PATH (type them bare).
- The AVD on this machine is named `Pixel_5`.
- A running emulator shows up to `adb` as `emulator-5554`.

For every Android action, follow the mobile-skill (`SKILL.md`) commands exactly.
